# SQLite database location (relative path — works on any machine).
# The db/custom.db file is committed with seeded demo data,
# so the app is runnable immediately after `bun install`.
DATABASE_URL="file:./db/custom.db"
